name: Flutter Build (Release APK - Error Tracked)

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          cache: true

      - name: Get Dependencies
        run: flutter pub get

      # بناء الـ Release بدون || true عشان لو فيه خطأ يظهر فوراً
      - name: Build Release APK
        run: flutter build apk --release --no-tree-shake-icons

      # خطوة التجميع (هتشتغل فقط لو البناء نجح)
      - name: Find and Collect APK
        run: |
          mkdir -p output
          find build/app/outputs/flutter-apk/ -name "*release.apk" -exec cp {} ./output/aksab_logistics_v2_release.apk \;
          ls -R output

      # الرفع النهائي
      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Aksab-Logistics-Release-v2
          path: output/*.apk
          if-no-files-found: error
